// very common data format in real world app
// especially for data retrieve 

let screenings = [

  { timestamp: 123, movieid, auditoriumID: 1},
  { timestamp: 123, movieid, auditoriumID: 1 }
  
]

let movies = [

  { id: 10, name: 'The hulk', desc: 'Great movie' },
  { id: 11, name: 'The poop', desc: 'Great movie' },
  
]

function search(phrase) {
  // filter array method runs a function once for every item in an array
  // and if the 
  let results = movies.filter(function (movie) {

    
  })
}
